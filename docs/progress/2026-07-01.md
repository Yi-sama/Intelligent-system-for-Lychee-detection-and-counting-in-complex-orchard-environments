- Updated the image-mode preview interaction in `app/gui.py`.
- Main preview click now toggles original/result display; the dedicated `预览` button opens the image dialog.
- Added wheel-based zoom support and resize-aware reflow in the preview dialog.
- Expanded `tests/test_app.py` with regression coverage for preview toggling, dialog opening, zoom behavior, and updated layout expectations.
- Verification: `conda run -p D:\Miniconda3\envs\yolo python -m unittest tests.test_app` passed (`Ran 17 tests ... OK`).
- Added `start.bat` at the project root to launch `app/main.py` from the fixed Conda environment with basic missing-environment error handling.
