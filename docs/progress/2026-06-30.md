# 2026-06-30 Progress

## Completed

- Built a minimal image-only prototype around local YOLO resources.
- Added a small application package with:
  - `app/infer.py` for image detection result normalization
  - `app/export.py` for CSV and annotated-image export
  - `app/gui.py` for a PyQt5 desktop window with image select, detect, CSV export, and result-image export
  - `app/main.py` as the desktop entry point
- Added `tests/test_app.py` using standard-library `unittest` so verification does not depend on missing `pytest` in the current `yolo` environment.
- Verified the local `D:\Miniconda3\envs\yolo` environment can import `PyQt5` and `ultralytics`.
- Verified real image inference using:
  - model: `Reference/yolo26n.pt`
  - sample: `Reference/TestFiles/000001_jpg.rf.9b4e3f4cd03429cadb85a914058f644f.jpg`
- Produced smoke-test outputs in `outputs/`:
  - `smoke_detections.csv`
  - `smoke_annotated.png`
- Summarized the next GUI-design direction for the PyQt5 desktop app.
- Confirmed the recommended next-step UI shape is a workbench-style layout:
  - left: input/control area
  - center: main preview area
  - right: result summary and detection details
- Confirmed scope discipline for the next UI iteration:
  - fully support image workflow first
  - reserve entry points for video and camera without forcing them into the first polished screen
- Reworked the GUI design direction based on user feedback:
  - switched away from a dense three-column workbench
  - locked in a preview-first layout with a large upper-right preview region
  - reduced the main action set to three icon-first controls: open image, run detection, export
  - confirmed direct interactions: click to toggle original/result image and mouse-wheel zoom
  - confirmed the bottom information strip should show both summary and detection details at the same time
- Completed another UI information-architecture refinement pass:
  - rejected the idea of stacking `conf`, `iou`, label toggles, and export controls into the top header
  - confirmed the new page structure as `top three mode blocks + thin status row + left configuration area + preview-local actions + bottom result area`
  - clarified that `图片`, `视频`, and `摄像头` should be visible as unified entry modes without implying equal implementation maturity in the current image-first iteration
- Rebuilt the PyQt5 GUI shell to match the confirmed structure:
  - added three top mode-entry blocks for `图片`, `视频`, and `摄像头`
  - added a dedicated status row for model, current file, state, and elapsed-time text
  - moved image parameters into a left configuration area and reserved video/camera configuration panels behind the mode switcher
  - moved core actions to a preview-local toolbar with `打开`, `开始检测`, `原图/结果`, and `导出`
  - moved result summary and detail presentation into a dedicated bottom result area
- Added GUI structure tests for the new layout contract and verified they pass in the local `yolo` environment.
- Refined the image-mode UI based on manual visual feedback:
  - spread the top status row into four separate metric cards
  - enlarged the image configuration controls and converted `conf` / `iou` labels to Chinese
  - changed the preview stage into a square-first image canvas to better handle portrait images
  - centered the four main preview actions and added click-to-open large-image preview
  - replaced the crowded bottom summary/detail strip with a shorter two-card result band for overview and detail only
  - kept the batch-image selector visible as a reserved entry, but intentionally did not present it as a finished workflow
- Reshaped the image-mode shell again into a true two-column layout:
  - left column now stacks `图片配置` above a compact `检测概览 + 检测明细` result card area
  - right column is now a single large preview card with tighter spacing between toolbar, image, and preview hint
  - enlarged the preview stage further to prioritize the image over surrounding whitespace

### Latest GUI Refinement

- Increased the right-side preview dominance by widening the two-column ratio and enlarging the square preview minimum size.
- Tightened spacing between the preview action bar, image canvas, and preview hint.
- Removed wasted vertical slack in the left configuration area so the lower result area moves upward and occupies more of the column.
- Fixed the detection-detail truncation so the detail list now shows all detections instead of only the first 6.
- Made preview scaling react to main-window resize events.
- Added a regression test to keep overview count and detail-list count aligned.

## Verification

- `conda run -p D:\Miniconda3\envs\yolo python -m unittest tests.test_app`
  - Result: `Ran 10 tests ... OK`
- `conda run -p D:\Miniconda3\envs\yolo python -m unittest tests.test_app`
  - Result after latest GUI refinement: `Ran 15 tests ... OK`
- `conda run -p D:\Miniconda3\envs\yolo python -c "... DetectionService(...).detect_image(...)"` against a real sample image
  - Result: inference completed successfully
  - Detection summary: `total=11`, `counts={'apple': 11}`
  - Output files written successfully under `outputs/`
- Re-read current GUI and docs constraints before UI planning:
  - `app/gui.py`
  - `app/main.py`
  - `docs/project-notes.md`
  - `docs/progress/2026-06-30.md`

## Follow-ups

- Launch the PyQt5 window and do one manual click-through check for image select, detect, and export.
- Replace the generic/local weight with a task-appropriate lychee detector if a trained project-specific model becomes available.
- Produce a concrete PyQt5 GUI design spec before the next substantial UI refactor.
- Redraw the GUI mockup to match the confirmed preview-first layout before refactoring `app/gui.py`.

## Completed Entry 10:37

- 完成阶段性项目梳理：确认项目现阶段目标是基于 Reference/TestFiles 和 yolo 环境实现荔枝检测与计数桌面原型；确认用户选择 PyQt5；确认本地 yolo 环境已具备 PyTorch、Ultralytics、PyQt5；整理了 YOLO26 资源职责、三类推理工程差异、PyQt5 界面职责以及结果保存/计数/CSV 导出的设计要点。
