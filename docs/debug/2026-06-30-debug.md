# 2026-06-30 Debug Log

## Issues

- `pytest` was not available in the current `D:\Miniconda3\envs\yolo` environment.
- Tests using `TemporaryDirectory()` under `conda run` hit permission errors in the system temp directory.
- `conda run` emitted OpenCL vendor/temp-file warnings during command execution.

## Attempts

- Tried `conda run -p D:\Miniconda3\envs\yolo pytest ...`
  - Failed because `pytest` executable was not found.
- Tried `conda run -p D:\Miniconda3\envs\yolo python -m pytest ...`
  - Failed because the `pytest` module was not installed.
- Reworked tests to `unittest`.
- Reworked test temp output paths to `tests/.tmp/...` inside the workspace.

## Resolution

- Unit/integration verification now runs with:
  - `conda run -p D:\Miniconda3\envs\yolo python -m unittest tests.test_app`
- Workspace-local temp directories avoid the permission issue seen with system temp paths.
- OpenCL/temp-file warnings remain visible but did not block successful inference or test execution.

## Durable Findings To Promote

- The current `yolo` environment should be treated as capable of running the prototype, but not as a fully clean environment.
- Prefer `unittest` over `pytest` unless `pytest` is explicitly installed later.
- The current GUI already proves the image workflow path, so the next UI work should focus on structure and usability rather than inventing new capabilities first.
